apiVersion: v2
name: memory-oom
description: A minimal chart that starts a pod and progressively consumes memory until OOMKilled, useful for testing CrashLoopBackOff behavior.
type: application
version: 0.4.0
appVersion: "1.0.0"
